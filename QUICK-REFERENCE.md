# Quick Reference Guide

This guide helps you quickly find the right location for your scripts.

## I want to...

### Assess my environment before migration
→ `Migration/Pre-Migration/`

### Migrate devices from on-premises to Intune
→ `Migration/During-Migration/`

### Validate migration was successful
→ `Migration/Validation/`

### Clean up after migration
→ `Migration/Post-Migration/`

### Manage Intune devices and policies
→ `Cloud/Intune-Management/`

### Work with Azure AD / Entra ID
→ `Cloud/Entra-ID/`

### Manage conditional access policies
→ `Cloud/Conditional-Access/`

### Deploy compliance policies
→ `Cloud/Compliance-Policies/`

### Manage Active Directory objects
→ `On-Premises/Active-Directory/`

### Work with Group Policy
→ `On-Premises/Group-Policy/`

### Manage SCCM / Configuration Manager
→ `On-Premises/SCCM/`

### Use Microsoft Graph API for devices
→ `GraphAPI/Device-Management/`

### Use Microsoft Graph API for users
→ `GraphAPI/User-Management/`

### Use Microsoft Graph API for apps
→ `GraphAPI/App-Management/`

### Use Microsoft Graph API for policies
→ `GraphAPI/Policy-Management/`

### Generate reports and analytics
→ `GraphAPI/Reporting/` (for Graph API-based reports)

### Generate compliance reports
→ `Reporting/Compliance-Reports/`

### Generate device inventory reports
→ `Reporting/Device-Reports/`

### Generate user reports
→ `Reporting/User-Reports/`

### Generate application reports
→ `Reporting/Application-Reports/`

### Generate security reports
→ `Reporting/Security-Reports/`

### Create custom or executive reports
→ `Reporting/Custom-Reports/`

### Remove stale devices or unused resources
→ `Maintenance/Cleanup/`

### Monitor environment health
→ `Maintenance/Monitoring/`

### Audit configurations and changes
→ `Maintenance/Audit/`

### Optimize performance and costs
→ `Maintenance/Optimization/`

## Common Workflows

### New to Intune Migration?
1. Start with `Migration/Pre-Migration/` - Assess your environment
2. Review `Migration/README.md` - Understand migration phases
3. Use `Migration/During-Migration/` - Execute migration
4. Verify with `Migration/Validation/` - Confirm success
5. Maintain with `Maintenance/` - Ongoing operations

### Already Using Intune?
1. Check `Cloud/` folders for management scripts
2. Use `GraphAPI/` for programmatic operations
3. Use `Reporting/` for comprehensive reports
4. Implement `Maintenance/` for ongoing optimization

### Hybrid Environment?
1. Use `On-Premises/` for on-prem infrastructure
2. Use `Cloud/` for cloud resources
3. Reference `Migration/` for transition scenarios

## Need Help?

- Check the README.md in each folder for detailed information
- Review CONTRIBUTING.md for script requirements
- Open an issue for questions or suggestions
